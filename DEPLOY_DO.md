# Инструкция по деплою на DigitalOcean

## Вариант 1: DigitalOcean App Platform (рекомендуется)

### Быстрый старт

#### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены в Git:
```bash
git add .
git commit -m "feat: prepare for DigitalOcean deployment"
git push origin main
```

#### 2. Создание аккаунта и подключение репозитория

1. Зайдите на [digitalocean.com](https://digitalocean.com) и создайте аккаунт (или войдите)
2. Перейдите в **App Platform** (в меню слева)
3. Нажмите **"Create App"**
4. Подключите ваш GitHub/GitLab репозиторий:
   - Если репозиторий еще не подключен, нажмите **"Connect GitHub"** / **"Connect GitLab"** и авторизуйтесь
   - Выберите репозиторий `auto_stop_loss_2.0`
   - Выберите ветку `main`

#### 3. Настройка сервиса

**Основные настройки:**
- **App Name**: `binance-loss-guardian` (или любое другое имя)
- **Region**: Выберите ближайший регион (например, `Frankfurt`, `New York`, `Singapore`)
- **Resource Type**: Выберите **"Web Service"**

**Настройки сборки:**
- **Build Command**: Оставьте пустым (Dockerfile сам все соберет)
- **Run Command**: Оставьте пустым (используется CMD из Dockerfile)
- **Dockerfile Path**: `./Dockerfile`
- **Docker Context**: `.` (корень проекта)

**Настройки запуска:**
- **HTTP Port**: `3000`
- **Health Check Path**: `/health`
- **Health Check Protocol**: `HTTP`

**План:**
- **Basic Plan**: $5/мес (512MB RAM, 1 vCPU) - минимум для работы 24/7
- **Professional Plan**: $12/мес (1GB RAM, 1 vCPU) - рекомендуется
- **Professional Plan**: $24/мес (2GB RAM, 2 vCPU) - для больших нагрузок

#### 4. Настройка переменных окружения

⚠️ **КРИТИЧЕСКИ ВАЖНЫЕ ПЕРЕМЕННЫЕ** (обязательно добавить):

В разделе **"Environment Variables"**:

1. Нажмите **"Edit"** или **"Add Variable"** для каждой переменной

**Обязательные переменные:**

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `BINANCE_API_KEY` | Ваш API ключ от Binance | API ключ для доступа к Binance Futures |
| `BINANCE_API_SECRET` | Ваш API секрет от Binance | API секрет для подписи запросов |
| `MAX_LOSS_USD` | `100` (или ваш лимит) | Максимальный убыток перед закрытием позиции |
| `DRY_RUN` | `false` или `true` | `false` для реальной торговли, `true` для тестирования |

**Важные переменные** (рекомендуется добавить):

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `INTERVAL_MS` | `1000` | Интервал проверки позиций в миллисекундах |
| `SHOW_ALL` | `true` | Показывать все позиции в логах |

**Telegram уведомления** (опционально):

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `ENABLE_TELEGRAM_NOTIFICATIONS` | `true` | Включить Telegram уведомления |
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота | Токен от @BotFather |
| `TELEGRAM_CHAT_ID` | ID вашего чата | ID чата для уведомлений |

**Переменные с дефолтными значениями** (можно не добавлять):

- `RECV_WINDOW=5000` - окно получения запросов Binance
- `BASE_URL=https://fapi.binance.com` - URL Binance Futures API
- `HEALTHCHECK_PORT=3000` - порт для healthcheck
- `MAX_RESTART_ATTEMPTS=10` - максимум попыток перезапуска
- `RESTART_BACKOFF_MS=5000` - задержка между перезапусками (мс)
- `TELEGRAM_NOTIFICATION_INTERVAL_MS=15000` - интервал Telegram уведомлений (мс)

#### 5. Создание и деплой

1. После настройки всех параметров нажмите **"Create Resources"** или **"Deploy"**
2. DigitalOcean автоматически:
   - Соберет Docker образ (может занять 5-10 минут при первом деплое)
   - Запустит контейнер
   - Выполнит healthcheck на `/health`

3. Дождитесь завершения деплоя:
   - Статус изменится с `Building` → `Deploying` → `Running`
   - В логах появятся сообщения о запуске приложения

---

## Вариант 2: DigitalOcean Droplet (VPS)

### Быстрый старт

#### 1. Создание Droplet

1. Зайдите на [digitalocean.com](https://digitalocean.com)
2. Перейдите в **Droplets** → **"Create Droplet"**
3. Настройки:
   - **Image**: `Docker` (выберите версию Docker на Ubuntu)
   - **Plan**: 
     - **Basic**: $6/мес (1GB RAM, 1 vCPU) - минимум
     - **Basic**: $12/мес (2GB RAM, 1 vCPU) - рекомендуется
   - **Region**: Выберите ближайший
   - **Authentication**: SSH ключ или пароль
   - **Hostname**: `binance-loss-guardian`

4. Нажмите **"Create Droplet"**

#### 2. Подключение к серверу

После создания Droplet подключитесь по SSH:
```bash
ssh root@your-droplet-ip
```

#### 3. Установка Docker (если не выбрали образ с Docker)

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Проверка установки
docker --version
```

#### 4. Клонирование репозитория

```bash
# Установка Git (если не установлен)
apt install git -y

# Клонирование репозитория
cd /opt
git clone https://github.com/your-username/auto_stop_loss_2.0.git
cd auto_stop_loss_2.0
```

#### 5. Создание файла с переменными окружения

```bash
# Создание .env файла
nano .env
```

Добавьте переменные:
```env
BINANCE_API_KEY=ваш_api_ключ
BINANCE_API_SECRET=ваш_api_секрет
MAX_LOSS_USD=100
DRY_RUN=false
INTERVAL_MS=1000
SHOW_ALL=true
HEALTHCHECK_PORT=3000
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

#### 6. Сборка и запуск Docker контейнера

```bash
# Сборка образа
docker build -t binance-loss-guardian .

# Запуск контейнера
docker run -d \
  --name loss-guardian \
  --restart unless-stopped \
  --env-file .env \
  -p 3000:3000 \
  binance-loss-guardian
```

#### 7. Проверка работы

```bash
# Просмотр логов
docker logs -f loss-guardian

# Проверка статуса
docker ps

# Проверка healthcheck
curl http://localhost:3000/health
```

#### 8. Настройка автозапуска (опционально)

Контейнер уже настроен на автозапуск (`--restart unless-stopped`), но можно также создать systemd сервис:

```bash
# Создание systemd сервиса
nano /etc/systemd/system/loss-guardian.service
```

Добавьте:
```ini
[Unit]
Description=Binance Loss Guardian
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/auto_stop_loss_2.0
ExecStart=/usr/bin/docker start loss-guardian
ExecStop=/usr/bin/docker stop loss-guardian
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

Активация сервиса:
```bash
systemctl daemon-reload
systemctl enable loss-guardian
systemctl start loss-guardian
```

#### 9. Обновление приложения

```bash
cd /opt/auto_stop_loss_2.0

# Получение последних изменений
git pull

# Пересборка образа
docker build -t binance-loss-guardian .

# Остановка старого контейнера
docker stop loss-guardian
docker rm loss-guardian

# Запуск нового контейнера
docker run -d \
  --name loss-guardian \
  --restart unless-stopped \
  --env-file .env \
  -p 3000:3000 \
  binance-loss-guardian
```

---

## Проверка работы

### 1. Проверка логов

**App Platform:**
- DigitalOcean Dashboard → Ваш App → **"Runtime Logs"**

**Droplet:**
```bash
docker logs -f loss-guardian
```

Должны быть сообщения:
```
=== Binance Futures Loss Guardian (polling every 1000ms, max loss 100 USDT, dryRun=false) ===
Starting healthcheck server on port 3000...
```

### 2. Проверка healthcheck

**App Platform:**
```
https://your-app-name.ondigitalocean.app/health
```

**Droplet:**
```
http://your-droplet-ip:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "healthy": true,
  "lastTickTime": 1234567890,
  "consecutiveErrors": 0,
  "uptime": 3600,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### 3. Проверка работы с Binance

В логах должны появиться сообщения о позициях:
```
BTCUSDT    | ▲ LONG  | amt=0.100000 | PnL=+15.50 USDT
ETHUSDT    | ▼ SHORT | amt=1.500000 | PnL=-25.30 USDT
```

## Важные переменные окружения

### 🔴 Критически важные (обязательно)

| Переменная | Описание | Пример значения |
|------------|-----------|-----------------|
| `BINANCE_API_KEY` | API ключ от Binance Futures | `abc123...` |
| `BINANCE_API_SECRET` | API секрет от Binance Futures | `xyz789...` |
| `MAX_LOSS_USD` | Максимальный убыток перед закрытием позиции | `100` |
| `DRY_RUN` | Режим тестирования (не закрывает позиции) | `false` или `true` |

### 🟡 Важные (рекомендуется)

| Переменная | Описание | Дефолт |
|------------|-----------|--------|
| `INTERVAL_MS` | Интервал проверки позиций (мс) | `1000` |
| `SHOW_ALL` | Показывать все позиции в логах | `true` |
| `ENABLE_TELEGRAM_NOTIFICATIONS` | Включить Telegram уведомления | `false` |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | - |

## Мониторинг

### App Platform
- **Runtime Logs**: DigitalOcean Dashboard → Ваш App → **"Runtime Logs"**
- **Metrics**: DigitalOcean Dashboard → Ваш App → **"Metrics"**
- **Alerts**: Можно настроить алерты на метрики

### Droplet
```bash
# Просмотр логов
docker logs -f loss-guardian

# Статистика контейнера
docker stats loss-guardian

# Проверка использования ресурсов
htop
```

## Обновление приложения

### App Platform

Автоматически при push в репозиторий (если включен Auto-Deploy):
1. Сделайте изменения и закоммитьте:
   ```bash
   git add .
   git commit -m "feat: your changes"
   git push origin main
   ```
2. DigitalOcean автоматически обнаружит изменения и задеплоит

Или вручную:
- DigitalOcean Dashboard → Ваш App → **"Deployments"** → **"Create Deployment"**

### Droplet

См. раздел "Обновление приложения" в варианте 2 выше.

## Troubleshooting

### Сервис не запускается

**App Platform:**
- Проверьте логи в разделе **"Runtime Logs"**
- Проверьте переменные окружения
- Убедитесь, что Dockerfile корректен

**Droplet:**
```bash
# Проверка логов
docker logs loss-guardian

# Проверка статуса контейнера
docker ps -a

# Перезапуск контейнера
docker restart loss-guardian
```

### Healthcheck возвращает 503

**Причины:**
- Приложение не может подключиться к Binance API
- Неверные API ключи
- Превышен лимит ошибок

**Решение:**
- Проверьте `BINANCE_API_KEY` и `BINANCE_API_SECRET`
- Проверьте логи на ошибки подключения
- Убедитесь, что API ключи имеют права на Futures торговлю

### Позиции не закрываются

**Проверьте:**
1. `DRY_RUN` - не установлен ли в `true`?
2. Логи - есть ли ошибки при попытке закрытия?
3. Binance API - достаточно ли прав у API ключей?

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте API ключи в Git
- Используйте переменные окружения (в App Platform они зашифрованы)
- На Droplet: защитите `.env` файл (`chmod 600 .env`)
- Ограничьте права API ключей в Binance (только Futures, без вывода средств)
- Регулярно проверяйте логи на подозрительную активность
- Используйте `DRY_RUN=true` для первого тестирования

### Дополнительная безопасность для Droplet

```bash
# Настройка firewall
ufw allow 22/tcp    # SSH
ufw allow 3000/tcp # Healthcheck (или только для localhost)
ufw enable

# Отключение root логина (рекомендуется)
# Создайте нового пользователя и используйте его вместо root
```

## Сравнение вариантов

| Критерий | App Platform | Droplet |
|----------|-------------|---------|
| Простота настройки | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Управление | Автоматическое | Ручное |
| Стоимость | От $5/мес | От $6/мес |
| Гибкость | Ограниченная | Полная |
| Автоматический деплой | ✅ Да | ❌ Нет (нужно настроить) |
| Мониторинг | Встроенный | Нужно настроить |

**Рекомендация:** Используйте **App Platform** для простоты, или **Droplet** если нужен полный контроль.
