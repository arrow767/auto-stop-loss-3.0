# Инструкция по деплою на Render

## Быстрый старт

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены в Git:
```bash
git add .
git commit -m "feat: prepare for Render deployment"
git push origin main
```

Необходимые файлы уже готовы:
- ✅ `Dockerfile` - конфигурация Docker образа
- ✅ `.dockerignore` - исключения для Docker сборки

### 2. Создание аккаунта и подключение репозитория

1. Зайдите на [render.com](https://render.com) и создайте аккаунт (или войдите)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub/GitLab/Bitbucket репозиторий:
   - Если репозиторий еще не подключен, нажмите **"Connect account"** и авторизуйтесь
   - Выберите репозиторий `auto_stop_loss_2.0` из списка

### 3. Настройка сервиса

После подключения репозитория заполните настройки:

**Основные настройки:**
- **Name**: `binance-loss-guardian` (или любое другое имя)
- **Environment**: Выберите **`Docker`**
- **Region**: Выберите ближайший регион (например, `Frankfurt`, `Oregon`, `Singapore`)
- **Branch**: `main` (или ваша рабочая ветка)
- **Root Directory**: Оставьте пустым или укажите `.` (корень проекта)
- **Dockerfile Path**: `./Dockerfile`
- **Docker Context**: `.` (точка означает корень проекта)

**Настройки запуска:**
- **Health Check Path**: `/health`
- **Auto-Deploy**: `Yes` (автоматический деплой при push в выбранную ветку)

**План:**
- Для тестирования: `Free` (может "засыпать" после 15 минут неактивности)
- Для продакшена: `Starter` ($7/мес) или выше (рекомендуется для постоянной работы)

### 4. Настройка переменных окружения

⚠️ **КРИТИЧЕСКИ ВАЖНЫЕ ПЕРЕМЕННЫЕ** (обязательно добавить):

Перед созданием сервиса или после, в настройках сервиса:

1. Прокрутите страницу вниз до раздела **"Environment Variables"**
2. Нажмите **"Add Environment Variable"** для каждой переменной

**Обязательные переменные:**

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `BINANCE_API_KEY` | Ваш API ключ от Binance | API ключ для доступа к Binance Futures |
| `BINANCE_API_SECRET` | Ваш API секрет от Binance | API секрет для подписи запросов |
| `MAX_LOSS_USD` | `100` (или ваш лимит) | Максимальный убыток перед закрытием позиции |
| `DRY_RUN` | `false` или `true` | `false` для реальной торговли, `true` для тестирования |

**Важные переменные** (рекомендуется добавить):

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `INTERVAL_MS` | `1000` | Интервал проверки позиций в миллисекундах |
| `SHOW_ALL` | `true` | Показывать все позиции в логах |

**Telegram уведомления** (опционально, если нужны уведомления):

| Переменная | Значение | Описание |
|------------|----------|-----------|
| `ENABLE_TELEGRAM_NOTIFICATIONS` | `true` | Включить Telegram уведомления |
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота | Токен от @BotFather |
| `TELEGRAM_CHAT_ID` | ID вашего чата | ID чата для уведомлений |

**Переменные с дефолтными значениями** (можно не добавлять, если значения по умолчанию подходят):

Эти переменные имеют значения по умолчанию в коде, но можно переопределить:
- `RECV_WINDOW=5000` - окно получения запросов Binance
- `BASE_URL=https://fapi.binance.com` - URL Binance Futures API
- `HEALTHCHECK_PORT=3000` - порт для healthcheck
- `MAX_RESTART_ATTEMPTS=10` - максимум попыток перезапуска
- `RESTART_BACKOFF_MS=5000` - задержка между перезапусками (мс)
- `TELEGRAM_NOTIFICATION_INTERVAL_MS=15000` - интервал Telegram уведомлений (мс)

### 5. Создание и деплой сервиса

1. После настройки всех параметров и переменных окружения нажмите **"Create Web Service"**
2. Render автоматически:
   - Соберет Docker образ (может занять 5-10 минут при первом деплое)
   - Запустит контейнер
   - Выполнит healthcheck на `/health`

3. Дождитесь завершения деплоя:
   - Статус изменится с `Building` → `Deploying` → `Live`
   - В логах появятся сообщения о запуске приложения

## Проверка работы

### 1. Проверка логов

В Render Dashboard:
- Откройте ваш сервис
- Перейдите в раздел **"Logs"**
- Должны быть сообщения:
  ```
  === Binance Futures Loss Guardian (polling every 1000ms, max loss 100 USDT, dryRun=false) ===
  Starting healthcheck server on port 3000...
  ```

### 2. Проверка healthcheck

Откройте в браузере или через curl:
```
https://your-service.onrender.com/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "healthy": true,
  "lastTickTime": 1234567890,
  "consecutiveErrors": 0,
  "uptime": 3600,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### 3. Проверка работы с Binance

В логах должны появиться сообщения о позициях:
```
BTCUSDT    | ▲ LONG  | amt=0.100000 | PnL=+15.50 USDT
ETHUSDT    | ▼ SHORT | amt=1.500000 | PnL=-25.30 USDT
```

## Важные переменные окружения

### 🔴 Критически важные (обязательно)

| Переменная | Описание | Пример значения |
|------------|-----------|-----------------|
| `BINANCE_API_KEY` | API ключ от Binance Futures | `abc123...` |
| `BINANCE_API_SECRET` | API секрет от Binance Futures | `xyz789...` |
| `MAX_LOSS_USD` | Максимальный убыток перед закрытием позиции | `100` |
| `DRY_RUN` | Режим тестирования (не закрывает позиции) | `false` или `true` |

### 🟡 Важные (рекомендуется)

| Переменная | Описание | Дефолт |
|------------|-----------|--------|
| `INTERVAL_MS` | Интервал проверки позиций (мс) | `1000` |
| `SHOW_ALL` | Показывать все позиции в логах | `true` |
| `ENABLE_TELEGRAM_NOTIFICATIONS` | Включить Telegram уведомления | `false` |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | - |

### 🟢 Опциональные (можно не менять)

| Переменная | Описание | Дефолт |
|------------|-----------|--------|
| `RECV_WINDOW` | Окно получения запросов Binance | `5000` |
| `BASE_URL` | URL Binance Futures API | `https://fapi.binance.com` |
| `HEALTHCHECK_PORT` | Порт для healthcheck | `3000` |
| `MAX_RESTART_ATTEMPTS` | Максимум попыток перезапуска | `10` |
| `RESTART_BACKOFF_MS` | Задержка между перезапусками (мс) | `5000` |
| `TELEGRAM_NOTIFICATION_INTERVAL_MS` | Интервал Telegram уведомлений (мс) | `15000` |

## Автоматический перезапуск

Приложение автоматически перезапускается при:
- Критических ошибках в основном цикле
- Превышении лимита последовательных ошибок
- Непредвиденных исключениях

Максимум попыток перезапуска: 10 (настраивается через `MAX_RESTART_ATTEMPTS`)

## Мониторинг

### Логи
- **Render Dashboard** → Ваш сервис → **"Logs"**
- Фильтруйте по `[ALERT]`, `[ERR]`, `[WARN]` для быстрого поиска проблем

### Healthcheck
- Render автоматически проверяет `/health` каждые 30 секунд
- Если healthcheck падает, Render перезапустит сервис

### Метрики в healthcheck ответе
- `uptime` - время работы в секундах
- `consecutiveErrors` - количество последовательных ошибок
- `lastTickTime` - время последнего успешного тика
- `healthy` - статус здоровья приложения

## Обновление приложения

### Автоматический деплой

Если включен **Auto-Deploy** (рекомендуется):
1. Сделайте изменения в коде и закоммитьте:
   ```bash
   git add .
   git commit -m "feat: your changes"
   git push origin main
   ```
2. Render автоматически:
   - Обнаружит изменения в репозитории
   - Соберет новый Docker образ
   - Запустит новый контейнер
   - Старый контейнер будет остановлен после успешного запуска нового

### Ручной деплой

Если Auto-Deploy отключен или нужно задеплоить конкретный коммит:
1. В Render Dashboard откройте ваш сервис
2. Перейдите в раздел **"Manual Deploy"**
3. Выберите коммит из списка или нажмите **"Deploy latest commit"**
4. Дождитесь завершения деплоя

## Troubleshooting

### Сервис не запускается

**Проверьте:**
1. Логи в Render Dashboard - ищите ошибки
2. Переменные окружения - все ли обязательные переменные установлены?
3. Dockerfile - правильно ли указан путь?

### Healthcheck возвращает 503

**Причины:**
- Приложение не может подключиться к Binance API
- Неверные API ключи
- Превышен лимит ошибок

**Решение:**
- Проверьте `BINANCE_API_KEY` и `BINANCE_API_SECRET`
- Проверьте логи на ошибки подключения
- Убедитесь, что API ключи имеют права на Futures торговлю

### Приложение "засыпает" на Free плане

**Причина:** Free план на Render останавливает сервисы после 15 минут неактивности

**Решение:**
- Перейдите на платный план (Starter $7/мес)
- Или используйте внешний мониторинг для "пробуждения" сервиса

### Позиции не закрываются

**Проверьте:**
1. `DRY_RUN` - не установлен ли в `true`?
2. Логи - есть ли ошибки при попытке закрытия?
3. Binance API - достаточно ли прав у API ключей?

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте API ключи в Git
- Используйте переменные окружения в Render (они зашифрованы)
- Ограничьте права API ключей в Binance (только Futures, без вывода средств)
- Регулярно проверяйте логи на подозрительную активность
- Используйте `DRY_RUN=true` для первого тестирования на Render
